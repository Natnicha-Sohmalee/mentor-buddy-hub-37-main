import{createFileRoute,useNavigate}from"@tanstack/react-router";import{useState,type FormEvent}from"react";import{AppShell,Section}from"@/components/app-shell";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{Textarea}from"@/components/ui/textarea";import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from"@/components/ui/select";import{pageHead}from"@/lib/head";import{insertRow}from"@/lib/supabase-data";import{useRole}from"@/lib/role-context";
export const Route=createFileRoute("/leaves/request")({head:()=>pageHead("ยื่นคำขอลา","ส่งคำขอลา"),component:LeaveRequest});function LeaveRequest(){const{user}=useRole();const nav=useNavigate();const[date,setDate]=useState(""),[type,setType]=useState("personal"),[reason,setReason]=useState(""),[error,setError]=useState("");const submit=async(e:FormEvent)=>{e.preventDefault();if(!user)return;try{await insertRow("leave_requests",{trainee_id:user.id,leave_date:date,leave_type:type,reason});await nav({to:"/"})}catch(x){setError(x instanceof Error?x.message:"ส่งคำขอไม่สำเร็จ")}};return <AppShell title="ยื่นคำขอลา" description="ส่งให้ Mentor/Admin พิจารณา"><Section><form className="grid max-w-xl gap-4" onSubmit={submit}><div><Label>วันที่ลา</Label><Input type="date" value={date} onChange={e=>setDate(e.target.value)} required/></div><div><Label>ประเภท</Label><Select value={type} onValueChange={setType}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="sick">ลาป่วย</SelectItem><SelectItem value="personal">ลากิจ</SelectItem><SelectItem value="other">อื่น ๆ</SelectItem></SelectContent></Select></div><div><Label>เหตุผล</Label><Textarea value={reason} onChange={e=>setReason(e.target.value)}/></div>{error&&<p className="text-sm text-destructive">{error}</p>}<Button>ส่งคำขอลา</Button></form></Section></AppShell>}
