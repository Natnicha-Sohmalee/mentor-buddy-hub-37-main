import{createFileRoute,useNavigate}from"@tanstack/react-router";import{useState,type FormEvent}from"react";import{AppShell,Section}from"@/components/app-shell";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{Textarea}from"@/components/ui/textarea";import{pageHead}from"@/lib/head";import{insertRow}from"@/lib/supabase-data";import{useRole}from"@/lib/role-context";
export const Route=createFileRoute("/extensions/request")({head:()=>pageHead("ขอขยายเวลา","ส่งคำขอขยายเวลาฝึกงาน"),component:ExtensionRequest});function ExtensionRequest(){const{user}=useRole();const nav=useNavigate();const[end,setEnd]=useState(""),[reason,setReason]=useState(""),[error,setError]=useState("");const submit=async(e:FormEvent)=>{e.preventDefault();if(!user)return;try{await insertRow("extension_requests",{trainee_id:user.id,requested_end_date:end,reason});await nav({to:"/"})}catch(x){setError(x instanceof Error?x.message:"ส่งคำขอไม่สำเร็จ")}};return <AppShell title="ขอขยายเวลาฝึกงาน" description="ส่งให้ผู้ดูแลพิจารณา"><Section><form className="grid max-w-xl gap-4" onSubmit={submit}><div><Label>วันสิ้นสุดใหม่ที่ต้องการ</Label><Input type="date" value={end} onChange={e=>setEnd(e.target.value)} required/></div><div><Label>เหตุผล</Label><Textarea value={reason} onChange={e=>setReason(e.target.value)} required/></div>{error&&<p className="text-sm text-destructive">{error}</p>}<Button>ส่งคำขอ</Button></form></Section></AppShell>}
